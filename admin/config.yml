backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)
media_folder: 'assets/uploads'
collections:
  - name: 'articles'
    label: 'Articles'
    folder: '_articles/'
    fields:
      - { name: Title }